### Example .env for packages/api
# Option A: MongoDB Atlas (replace <USER>, <PASSWORD>, <CLUSTER>, <DBNAME>)
# URL-encode any special characters in the username/password
MONGO_URI=mongodb+srv://<USER>:<PASSWORD>@<CLUSTER>.mongodb.net/<DBNAME>?retryWrites=true&w=majority&appName=Cluster0

# Option B: Local Mongo (fast for local dev)
# MONGO_URI=mongodb://127.0.0.1:27017/aytekdb

# Optional DB name override (mongoose connect will use DB from URI; set to none to use URI DB)
# MONGO_DB=aytekdb

# PORT the API should listen on (default 5000)
# PORT=5000

# Notes:
# - For Atlas: whitelist your IP (or allow 0.0.0.0/0 for temporary testing).
# - If you get a DNS/`querySrv EBADNAME` error, ensure you replaced the <CLUSTER> placeholder
#   with the real cluster host name shown in Atlas (example: cluster0).
